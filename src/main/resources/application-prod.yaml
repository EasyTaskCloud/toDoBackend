server:
  port: 9000

spring:
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/easytaskdb
    username: admin
    password: pwd

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  flyway:
    locations:
      db/migration/common
      db/migration/prod
    clean-disabled: true

logging:
  level:
    root: INFO

app:
  cors:
    allowed-origins: "http://localhost:8080"
    allowed-methods: "GET,POST,PUT,DELETE"
